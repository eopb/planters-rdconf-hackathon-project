struct World {
    positions: Vec<Option<(f64, f64)>>,
    velocities: Vec<Option<(f64, f64)>>,
    attracting: Vec<Option<bool>>,
}

impl World {
    fn simulate(&self) {
        for (idx, pos) in positions.iter().enumerate() {}
    }
}
